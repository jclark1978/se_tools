# Deploy Key

This folder is intentionally ignored by git.

Use `se_tools_deploy_key.pub` as the GitHub deploy key value in the repository settings. Keep `se_tools_deploy_key` private and do not commit it.

